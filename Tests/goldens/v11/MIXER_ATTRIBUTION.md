# HSL mixer golden attribution

The `color-mixer` cases are additive render-v10 baselines for the HSL color
mixer. They were generated on win-x64 from the canonical Canon EOS 350D RAW
and Display-P3 fixtures. All pre-existing v10 settings cases remain unchanged;
an absent mixer still follows the frozen render-v10 path byte for byte.

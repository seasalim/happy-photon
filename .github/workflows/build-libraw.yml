name: Build LibRaw native artifacts

# Placeholder registered on main so GitHub will dispatch this workflow by
# name with --ref pointing at a build branch, where the full matrix lives.
# Replaced by the reviewed full workflow when that branch merges.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  placeholder:
    runs-on: ubuntu-22.04
    steps:
      - name: Explain placeholder
        run: echo "Dispatch this workflow with --ref <build branch> to run the real native build matrix."
